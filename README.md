**Photon2-Cloud-Thermostat
**
Photon 2 based thermostat that reads sensor value to local webpage where user can select state and see temperature.

In future, would like to add CSS so the site is more user friendly, and implement a recommended state algorithm based on set temperature vs actual.
